{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "$lib": ["../src/lib"],
      "$lib/*": ["../src/lib/*"],
      "$env": ["./mocks/env"],
      "$env/*": ["./mocks/env/*"]
    }
  }
}
